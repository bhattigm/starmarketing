import Link from 'next/link';
import styles from './index.module.scss'

const Menu = () => {
    return (
        <link>
            <a >Home</a>
        </link>
    );
};

export default Menu;